@model IEnumerable<ClinicDent.Models.Historial_Clinico>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.fecha_registro)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tipo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Consultas_Tratamientos.id_consulta_tratamiento)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.fecha_registro)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tipo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Consultas_Tratamientos.id_consulta_tratamiento)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_historial }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_historial }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_historial })
        </td>
    </tr>
}

</table>
